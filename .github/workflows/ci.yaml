name: Lavandula

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: [ ubuntu-latest, macos-latest ]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        run: make

      - name: Test
        run: make test